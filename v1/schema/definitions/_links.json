{
    "_links": {
        "type": "object",
        "required": [
            "self"
        ],
        "properties": {
            "self": {
                "$ref": "#/Link",
                "description": "A link which returns the entity itself"
            },
            "additionalProperties": {
                "$ref": "#/Link"
            }
        }
    },
    "Link": {
        "type": "object",
        "required": [
            "href"
        ],
        "properties": {
            "href": {
                "type": "string",
                "format": "url",
                "description": "URL of link"
            }
        }
    }
}